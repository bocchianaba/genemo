import { CamerounDatePipe } from './cameroun-date.pipe';

describe('CamerounDatePipe', () => {
  it('create an instance', () => {
    const pipe = new CamerounDatePipe();
    expect(pipe).toBeTruthy();
  });
});
